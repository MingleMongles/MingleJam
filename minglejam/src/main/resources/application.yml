spring:
  application:
    name: minglejam

  server:
    port: 8080

  datasource:
    url: jdbc:postgresql://localhost:5432/minglejamdb
    username: minglejam
    password: jinjin
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    database: postgresql
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false

mybatis:
  mapper-locations: classpath:/mappers/**/*.xml